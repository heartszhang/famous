<UserControl x:Class="famousfront.designtime.Prototype"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ff="clr-namespace:famousfront"
             xmlns:ctrl="clr-namespace:famousfront.controls"
             xmlns:utils="clr-namespace:famousfront.utils"
             xmlns:vm="clr-namespace:famousfront.viewmodels" d:DesignWidth="800"
             xmlns:metro="http://schemas.codeplex.com/elysium"
             xmlns:params="http://schemas.codeplex.com/elysium/params" 
             params:Design.Theme="Light"
  params:General.TextFontSize="{StaticResource FiveTextSize}"
  params:General.ContentFontSize="{StaticResource LittleFourTextSize}"
  params:General.TitleFontSize="{StaticResource FourTextSize}"
  params:General.HeaderFontSize="{StaticResource LittleThreeTextSize}"
             TextElement.FontFamily="{StaticResource BoldFont}"      
             mc:Ignorable="d" d:DesignHeight="300" Background="white">
  <UserControl.Resources>
  </UserControl.Resources>
  <Grid x:Name="LayoutRoot">
    <DocumentViewer>
    </DocumentViewer>
  </Grid>
</UserControl>
