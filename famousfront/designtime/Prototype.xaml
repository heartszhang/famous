<UserControl x:Class="famousfront.designtime.Prototype"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ff="clr-namespace:famousfront"
             xmlns:ctrl="clr-namespace:famousfront.controls"
             xmlns:utils="clr-namespace:famousfront.utils"
             xmlns:vm="clr-namespace:famousfront.viewmodels" d:DesignWidth="600"
             xmlns:metro="http://schemas.codeplex.com/elysium"
             xmlns:params="http://schemas.codeplex.com/elysium/params" 
  params:General.ContentFontSize="{StaticResource NormalTextSize}"
  params:General.HeaderFontSize="{StaticResource LargeTextSize}"
  params:General.TitleFontSize="{StaticResource VeryLargeTextSize}"             
             TextElement.FontFamily="{StaticResource LightFont}"      
             d:DesignHeight="454" 
             mc:Ignorable="d" Background="White">
  <UserControl.Resources>
  </UserControl.Resources>
  <Grid x:Name="LayoutRoot" >
    <Grid Width="70" HorizontalAlignment="Right" Background="{DynamicResource HighlightBrush}">
      <Grid.RowDefinitions>
        <RowDefinition Height="70"/>
        <RowDefinition Height="70"/>
        <RowDefinition Height="70"/>
        <RowDefinition Height="70"/>
        <RowDefinition/>
        <RowDefinition/>
        <RowDefinition/>
        <RowDefinition Height="20"/>
      </Grid.RowDefinitions>
      <Button Grid.Row="0" />
    </Grid>
  </Grid>
</UserControl>
