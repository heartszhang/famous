<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:famousfront.viewmodels" 
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                    xmlns:params="http://schemas.codeplex.com/elysium/params"
                    xmlns:ctrl="clr-namespace:famousfront.controls"
                    mc:Ignorable="d">
    <DataTemplate DataType="{x:Type vm:FeedEntryViewModel}" d:IsDesignTimeCreatable="false">
    <DataTemplate.Resources>
      <Style TargetType="{x:Type FlowDocument}" x:Key="{x:Type FlowDocument}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
      </Style>
    </DataTemplate.Resources>
    <StackPanel >
      <TextBlock x:Name="_title" Text="{Binding Title}" FontSize="{Binding (params:General.HeaderFontSize), RelativeSource={RelativeSource self}}" />
      <!--<TextBlock Text="{Binding Summary}" TextWrapping="Wrap" />-->
      <ctrl:FlowTextBlock Document="{Binding Summary}" Style="{StaticResource {x:Type RichTextBox}}"
                          IsReadOnly="True" BorderThickness="0" FocusVisualStyle="{x:Null}" 
                          Background="Transparent"
                          Foreground="{Binding Foreground, ElementName=_title, Mode=OneWay}"/>
    </StackPanel>
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding FontSize, ElementName=_title, Mode=OneWay,Converter={StaticResource IsGreaterThanConverter}, ConverterParameter=18}" Value="True">
        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal" TargetName="_title"/>
      </DataTrigger>

    </DataTemplate.Triggers>
  </DataTemplate>
</ResourceDictionary>